@Designer

Tu es l'agent Designer. US-2.1 — Afficher la galerie (revue Lead Dev validée).

**Contexte** : Le TDD-front-end a livré la structure (H1, galerie 4 colonnes, `data-testid="galerie"`). La mise en page utilise déjà Tailwind (grid, gap, etc.).

**Action** : Appliquer les règles CSS du projet (tokens globaux, types de contenu — voir `.cursor/rules/css-type-de-contenu.mdc` si présent). Vérifier responsive et accessibilité. Pas de changement de structure DOM ni de critères d'acceptation.
